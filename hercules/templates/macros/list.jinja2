{# Card #}
{%- macro card(title='') -%}
    <div class="card mb-3">
        {% if title %}<div class="card-header">
            {{ title }}
        </div>{% endif %}
        <div class="card-body">
            {{ caller () }}
        </div>
    </div>
{%- endmacro -%}

{# JavaScript Datatable #}
{%- macro datatable(table_id, order="asc") -%}
    <!-- Datatable -->
    <script>
        $('#{{ table_id }}').DataTable({
            {% if order == "asc" %}order: [[ 0, "asc" ]],{% endif %}
            {% if order == "desc" %}order: [[ 0, "desc" ]],{% endif %}
            language: {
                lengthMenu: "Mostrar _MENU_",
                search: "Filtrar:",
                zeroRecords: "No se encontraron registros",
                info: "Total de registros _TOTAL_ ",
                infoEmpty: "No hay registros",
                infoFiltered: "(_TOTAL_ filtrados de _MAX_ registros)",
                oPaginate: {
                    sFirst: "Primero",
                    sLast: "Último",
                    sNext: "Siguiente",
                    sPrevious: "Anterior"
                }
            },
            responsive: true
        });
    </script>
{%- endmacro -%}

{# JavaScript configDataTable #}
{%- macro config_datatable() -%}
    <script>
        let configDataTable = {
            processing: true,
            serverSide: true,
            ordering: false,
            searching: false,
            responsive: true,
            scrollX: true,
            ajax: {
                url: null,
                type: "POST",
                headers: { "X-CSRF-TOKEN": "{{ csrf_token() }}" },
                dataType: "json",
                dataSrc: "data",
                data: null
            },
            columns: null,
            columnDefs: null,
            language: {
                lengthMenu: "Mostrar _MENU_",
                search: "Filtrar:",
                zeroRecords: "No se encontraron registros",
                info: "Total de registros _TOTAL_ ",
                infoEmpty: "No hay registros",
                infoFiltered: "(_TOTAL_ filtrados de _MAX_ registros)",
                oPaginate: {
                    sFirst: "Primero",
                    sLast: "Último",
                    sNext: "Siguiente",
                    sPrevious: "Anterior"
                }
            }
        }
    </script>
{%- endmacro -%}

{# Button small #}
{%- macro button_sm(label, url, icon='mdi:file', target='', color_class='btn-outline-primary') -%}
    <a class="btn btn-block btn-sm {{ color_class }} w-100 my-2" href="{{ url }}" {% if target %}target="{{ target }}"{% endif %}>
        {% if icon %}<span class="iconify" data-icon="{{ icon }}"></span>{% endif %}
        {{ label }}
    </a>
{%- endmacro -%}

{# Button medium #}
{%- macro button_md(label, url, icon='mdi:file', target='', color_class='btn-outline-primary') -%}
    <a class="btn btn-block btn-md {{ color_class }} w-100 my-2" href="{{ url }}" {% if target %}target="{{ target }}"{% endif %}>
        {% if icon %}<span class="iconify" data-icon="{{ icon }}"></span>{% endif %}
        {{ label }}
    </a>
{%- endmacro -%}

{# Button large #}
{%- macro button_lg(label, url, icon='mdi:file', target='', color_class='btn-outline-primary') -%}
    <a class="btn btn-block btn-lg {{ color_class }} w-100 my-2" href="{{ url }}" {% if target %}target="{{ target }}"{% endif %}>
        {% if icon %}<span class="iconify" data-icon="{{ icon }}"></span>{% endif %}
        {{ label }}
    </a>
{%- endmacro -%}
